appId: com.harry-expo.bluewallet
---
- tapOn:
    id: ${SCAN_TAP_ID}

- runFlow:
    when:
      visible: "Allow" # TODO: check for android too
    commands:
      - tapOn: "Allow"

# wait for camera screen to initialize
- waitForAnimationToEnd:
    timeout: 5000