appId: com.harry-expo.bluewallet
name: "Clean install and run"
---
- launchApp:
    clearState: true
    clearKeychain: true
    permissions: 
      all: unset

- runFlow: 
    when:
      true: ${DEV_CLIENT === '1'}
    file: 'debug-run.yaml'

- runFlow: 'assert-wallets-list.yaml'