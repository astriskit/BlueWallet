appId: com.harry-expo.bluewallet
---
- repeat:
    while:
      notVisible:
        id: ${SWIPE_ID_NOT_VISIBLE}
    commands:
      - swipe:
          from:
            id: ${SWIPE_ID_FROM}
          start: 80%, 25%
          end: 30%, 25%

- assertVisible:
    id: ${SWIPE_ID_NOT_VISIBLE}